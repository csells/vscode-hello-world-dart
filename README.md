# VSCode hello-world-dart extension

Extension's main file is `bin/extension.dart`.

## Building

```bash
$ dart compile js bin/extension.dart --out extension.js
```

## Testing

Open extension.js and hit `F5` to test it.
